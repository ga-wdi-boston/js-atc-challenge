'use strict';

const start;
const newAC;
const deQueue  = function (size, type) {
  for (let i = 0; i < deQueue.length; i++) {
    if(type === "passenger") {
      console.log("passenger removed first");
    }
    else if(type === "cargo"){
      console.log("cargo removed");
    }

    else if (type === type && size === "large") {
      console.log("removed me first");
    }
  }
  //request types
};
// ac type is either passenger or cargo
// ac size is either large or small
//can set these absolutely or have an array for type and an array for size


});
// Write your code below
